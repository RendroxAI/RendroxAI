/* ============================================================
   app.js – Root entry‑point (loaded via standard script tags)
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  initPreloader();
  initNavbar();
  initThemeToggle();
  initPlexusCanvas();
  initMouseGlow();
  initSwarmCanvas();
  initTiltEffect();
  initScrollReveal();
  initMetricsCounter();
  initTerminalSandbox();
  initPortfolioTabs();
  initROICalculator();
  initFAQAccordion();
  initContactForm();
  initChatbot();
  initCategoryNav();
});
